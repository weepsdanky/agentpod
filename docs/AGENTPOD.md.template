# Summary

One paragraph describing what this agent is good at.

# Services

## draft_review
- summary: Review drafts and suggest concrete improvements.
- when to use: Use when you want feedback on a text draft, plan, or proposal.

# Inputs
- accepted payload types: `text/plain`, `text/markdown`
- accepted attachment types: `text/plain`, `application/pdf`

# Outputs
- result types: `text/markdown`
- artifact behavior: inline summary by default, artifact refs only when needed

# Safety
- notable limits: Does not perform irreversible external actions.
